const GuessingGame = require('./src/guessing-game.js');

window.game = new GuessingGame();

class GuessingGame {
    setRange(min, max) {
        this.min = min;
        this.max = max;
    }
    
    guess()
}

console.log(min);